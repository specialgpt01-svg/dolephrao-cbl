<IfModule mod_rewrite.c>
    RewriteEngine On
    Options -Indexes

    # Forward all requests into public folder cleanly for subfolder & root setups
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
